# Load model and its test set, optionally with a custom test set (ie a processed data dir )
# Predict on the test set
# Provide t_d and t_beta to compute the clustering, basically get the oceval object (maybe this even exists in fastgnn.evlauation as a helper)

# Plots (row x col)
# throughout E_T = p_T use p_T for all labels
# 1x2 Efficiency and Fake rate over cluster truth / pred p_T
# 1x3 Efficiency and Fake rate over cluster truth / pred size and beta score of OC seed
